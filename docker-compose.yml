services:
  pivo:
    image: dockerjustin98/pivo:latest
    # For local development, comment out `image:` above and uncomment `build:` below.
    # build: .
    container_name: pivo
    restart: unless-stopped
    ports:
      - "1998:1998"
    environment:
      TZ: ${TZ:-America/New_York}
      # ADMIN_PASSWORD: ${ADMIN_PASSWORD:-}
    volumes:
      - ./data:/data
